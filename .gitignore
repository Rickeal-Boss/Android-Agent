# =============================================================================
# LiquidAgent / Android-Agent
# =============================================================================

# -----------------------------------------------------------------------------
# 构建产物
# -----------------------------------------------------------------------------
# ⚠️ Wave4 审查（D-P2-3）：目录名规则必须锚定仓库根（加前导 /）——不带 / 的
# `release/`、`outputs/` 会匹配**任何层级**的同名目录：`app/src/release/Foo.kt`、
# `core-design/.../release/Bar.kt` 都被静默吞掉，本地 git add 漏文件、CI fresh
# checkout 必然缺文件（「本地绿 CI 红」的头号根因）。build/ 保留通配（各模块都有）。
*.apk
*.aab
*.ap_
*.dex
build/
/captures/
.cxx/
/release/
/outputs/
*.log

# -----------------------------------------------------------------------------
# Gradle
# -----------------------------------------------------------------------------
.gradle/
local.properties
gradle-app.setting
!gradle-wrapper.jar
.gradletasknamecache

# -----------------------------------------------------------------------------
# 签名密钥（绝不允许入库）
# -----------------------------------------------------------------------------
*.jks
*.keystore
*.p12
*.pfx
!debug.keystore
signing.properties
keystore.properties
*.gpg
*.kdbx

# -----------------------------------------------------------------------------
# 端侧模型文件（体积巨大，一律从 HuggingFace 手动导入，见 README「模型获取」）
# -----------------------------------------------------------------------------
*.litertlm
*.task
*.gguf
*.bin
*.tflite
*.onnx
*.safetensors
*.pt
*.pth
*.npz
*.llamafile

# -----------------------------------------------------------------------------
# Android Studio / IntelliJ
# -----------------------------------------------------------------------------
*.iml
*.ipr
*.iws
.idea/
.idea/**/workspace.xml
.idea/**/tasks.xml
.idea/**/gradle.xml
.idea/**/dictionaries
.idea/**/libraries
out/

# -----------------------------------------------------------------------------
# 其他 IDE / 编辑器
# -----------------------------------------------------------------------------
.vscode/
*.swp
*.swo
*~

# -----------------------------------------------------------------------------
# 操作系统
# -----------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# -----------------------------------------------------------------------------
# Kotlin / 调试 / 覆盖率
# -----------------------------------------------------------------------------
.kotlin/
*.hprof
jacoco.exec

# deploy script build output
dist/
