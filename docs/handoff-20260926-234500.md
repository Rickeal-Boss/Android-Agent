# Handoff: Wave 21 交付 —— harness-improve 分支八项改进（反退化 v2 + 聊天复制 + 开关手感 + 覆盖层透明度）

> 生成于 2026-09-26 23:45。仓库 `D:\WBfil\2026-09-18-18-27-37\Android-Agent`，分支
> **`harness-improve`**，远端 HEAD **`6fa3659`**（CI Build/Release 双绿，产物齐）。
> 新会话开工前先读 `.workbuddy/memory/2026-09-26.md`（Wave 21 条目置顶）与本文件。

## Current state

用户八项需求全部落地，10 个 commit 推送，CI 三轮后双绿：

| Commit | 内容 |
|---|---|
| `18e280e` | 聊天可复制/复制为 Markdown（GlassBubble actions 插槽 + SelectionContainer 非流式正文）+ 新对话按钮 Edit→Add + Snackbar + 参数面板上抬（LocalBottomBarOverlay 避让悬浮页签）+ overlayOpacity scrim |
| `9b5fe66` | **反退化 v2**：短签名连击（保标点零门槛新口径，≥6 连击）/text 流 4096 无句界预算/提示词回显检测（连续≥2 句命中，指纹集 O(|prompt|) 构造一次）+ SmolVLM2 repPen 下限 1.05 |
| `d1daad7` | GlassSwitch 轨道色 2/3 行程映射（iOS 对齐）+ onDragStopped else 归位（让位/断流/亚 slop 三卡死路径）+ CyberJoystickSwitch 蓝色版移植（仅移动数据开关）+ overlayOpacity 全链路（GlassConfig→ThemeState→SettingsRepository→App→三覆盖层 scrim→设置页独立滑条） |
| `c215b0a` | Wave 20 交接文档入库 |
| `800d11e` | 审查 P1-2：复制按钮 48dp 触控热区 |
| `3dd0ea0` | 审查 P2-2：CyberJoystickSwitch TalkBack stateDescription |
| `08fbfd5` | 审查 P0-1/P1-1：TEXT_RUNAWAY_CHARS 常量补定义 + buildSystemSections 重构（回显指纹豁免记忆段，发送路径逐字节等价） |
| `56b5970` | CI 红修①：SelectionContainer import 包路径（正确：`androidx.compose.foundation.text.selection.SelectionContainer`） |
| `c6b5ba4` | CI：harness-improve 分支纳入 build.yml/release.yml 触发列表（此后 push 自动触发） |
| `6fa3659` | CI 红修②：ConversationDrawer scrim 补 Box import |

## 关键决策与依据（详见 commit 与 memory）

- **退化症状拆两段**（SmolVLM2-500M 真机）：回显段=buildSystemInstruction 工具段被当语料复读（采样防不住，是高概率复制）；退化段=「、」「当:」单字符行刷屏（旧检测器 8 字门槛+剥标点全盲）。
- **gallery 调研**：零循环防护可抄；其 agent 不回显的前提是原生 ToolProvider 通道（工具描述不进系统提示词）——本仓文本协议在 nativeToolChannel 验证前只能检测层兜底。
- **文献**：Fu 2021（高频词自强化→标点 run 是最早期形态）、Welleck 2020（repPen 副作用→下限只取 1.05）、Xu 2022（句级循环是解码系统性产物）。
- **NoRepeatNgram 不接**（裁定留档）：无差别禁 n-gram 对中文聊天多行短句误伤致命；留档"仅对近 N 轮触发 loop 的会话按白名单临时开 n=2~3"。
- **回显指纹豁免记忆段**：模型逐字引用记忆是合法行为（提示词自声明"参考资料"）；echoCorpus=指令性 section 过滤 MEMORY_SECTION_PREFIX。
- **GlassSwitch 归位根因**：onDragStopped 只在 `!yieldedToParent && finishedNormally && 拖过slop` 才提交，三条不提交路径没人管 fraction；修复=else 归位分支（提交分支不加归位，防弹两下）。
- **overlayOpacity 默认 0.45**：现状等效透出约 0.25，0.45 为可读性明显改善又不闷死玻璃的中点；三类覆盖层（推理参数面板/新增记忆 LiquidDialog/会话抽屉）统一 glassShadow×overlayOpacity scrim。

## CI 终态（SHA 6fa3659）

- Build run 36251926767 SUCCESS（3m48s）；Release run 36251926771 SUCCESS（5m00s）
- 产物：`liquidagent-release-apk-harness-improve` 69.96MB（签名正式包）/ `liquidagent-release-mapping-harness-improve` 4.20MB / `liquidagent-debug-harness-improve` 39.17MB
- **触发列表已补**：harness-improve 此后每次 push 自动跑双工作流

## Next steps（按优先级）

1. **真机验收 Wave 21**：
   - 反退化：SmolVLM2 复现原症状（应被 prompt_echo / short_sig_run 提前截断，已产出文本保留+注入提醒）；Qwen2.5-1.5B / Gemma 正常对话回归无误截；日志关键字 `prompt_echo` / `short_sig_run` / `text_runaway_sentence` / 「模型采样档案生效」。
   - 聊天：助手气泡下「复制 / 复制为 Markdown」两枚按钮（复制=净化文本、MD=原文）；长按选中正文（流式气泡不可选是设计）。
   - 开关：玻璃开关拖到 2/3 前不变色；设置页按住开关纵向滑列表后归位；移动数据开关为蓝色摇杆。
   - 覆盖层：设置页「覆盖层不透明度」滑条即时生效且重启保持；推理参数面板底边停在页签上沿不再重叠。
   - 新对话按钮 Add 图标 + Snackbar 反馈。
2. **留档事项**：P2-1（连续 ≥6 相同短行如 Markdown `---` 分隔线的理论误报面）真机回归长文档生成场景；ghapi.sh 内嵌旧 PAT 已失效待更新；Release debug 兜底与 Build job 重复构建可 artifact 复用；500M 级模型建议关闭工具调用（模型页副文案，未做代码）。
3. **开放项**（承接 Wave 20 复审）：E2 core-agent 零测试、A1 材质亮度自适应、A3 滚动深度驱动底栏材质、A7 同心圆角、A8 README 截图、THIN 0.21 vs KDoc ≤0.15（待用户裁决）、Liquid 融合容器（morph shader）独立波次、原生 ToolProvider 通道验证（根治回显的终解）。

## Relevant artifacts

- 仓库：`D:\WBfil\2026-09-18-18-27-37\Android-Agent`（branch `harness-improve`，本地 HEAD 与远端 6fa3659 一致，工作树干净）
- 会话记忆：`../.workbuddy/memory/2026-09-26.md`（Wave 21 条目置顶）
- 常驻交接：`../HANDOVER-harness.md`；CI 日志样本 `../job2_build.log` `../job_build.log`
- CI 工具：`../_ci-tools/`（ghapi.sh 内嵌 PAT 已失效——本轮用 curl 直连 api.github.com，必须 `--ssl-no-revoke -L` + Bearer）
- GitHub：`Rickeal-Boss/Android-Agent`，分支 `harness-improve`，HEAD `6fa3659`
